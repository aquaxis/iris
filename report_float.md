# 浮動小数点の対応の検討

## この文書について

指示はこう言っている。

```
浮動小数点を対応するためにフォーマットをrustや現在のAIやLLMの情勢から検討して下さい
```

**これは検討である。実装はしていない。**
どの形式を、どういう理由で採るか（あるいは採らないか）の判断材料を並べる。

置き場所は指示が名指していない。
だがアプリの検討を`report_apps.md`に置けと言っており、検討は作業の記録である。
そこで同じ形に揃え、この`report_float.md`に報告する。
**この置き場所の名は当方の選択であって、指示が名指したものではない。**

情勢の事実は2026-08-22にWebで調べた。出典は末尾に置く。

## 1. なぜ検討するか

**IRISは浮動小数点を持たない。**

```
tools/veryl2iris/mapping/src/lib.rs:
  m("f32 / f64", "no counterpart", Verdict::Unsupported,
    "IRIS has no floating point type and no real literal"),
```

Verylは`f32`／`f64`とposit（`p8`〜`p64`）と実数リテラルを持つ。
IRISはどれも持たないので、相互変換で落ちる。

そしてハードウェアの用途が浮動小数点に寄っている。
機械学習の加速器は低精度の浮動小数点で動く。
IRISがその領域を書けるかは、どの形式を持つかで決まる。

## 2. 候補の形式

| 形式 | 構成（符号/指数/仮数） | 幅 | 由来 |
|---|---|---|---|
| f64 | 1 / 11 / 52 | 64 | IEEE 754倍精度。Rustの`f64` |
| f32 | 1 / 8 / 23 | 32 | IEEE 754単精度。Rustの`f32` |
| f16 | 1 / 5 / 10 | 16 | IEEE 754半精度。Rustは`half`クレート |
| bf16 | 1 / 8 / 7 | 16 | 指数がf32と同じ。機械学習 |
| fp8 E4M3 | 1 / 4 / 3 | 8 | 機械学習の推論・訓練 |
| fp8 E5M2 | 1 / 5 / 2 | 8 | 同上。指数を広く |
| fp4 (E2M1) | 1 / 2 / 1 | 4 | MXFP4／NVFP4の要素 |
| posit | 別系統（regime可変） | 8〜64 | Verylが持つ |

### 2.1 マイクロスケーリング（MX）は複合である

OCPのMX仕様は、32要素で1つのスケール（E8M0）を共有する**ブロック形式**である。
要素はMXFP4（S1E2M1）、MXFP6、MXFP8などがある。
NVIDIAのNVFP4は16要素ブロックで、スケールにFP8 E4M3を使う。

**これは単一の型ではなく、狭い浮動小数点の配列と共有スケールの組である。**
だからIRISでは基本型ではなく、`struct`と配列の組み合わせで表すのが素直である。
基本型として持つべきは、その要素（fp8、fp4）のほうである。

## 3. 比べる基準

| 基準 | なぜ見るか |
|---|---|
| ビット表現が確定しているか | HDLは決定的なビット列を要る。IEEE 754とMXは確定している |
| 幅 | 面積と帯域に直結する |
| 既存の型体系との整合 | IRISは`bit[N]`／`int`／`uint`を持つ。浮動小数点はその上にどう載るか |
| 模擬実行の重さ | シミュレータが演算を模す必要がある |
| 形式的等価性への影響 | 浮動小数点の等価性はビット単位でしか言えない |
| 情勢との合致 | いまハードウェアが実装している形式か |

## 4. 現在のAIとLLMの情勢

2026-08-22に調べた。

| 事実 | 内容 |
|---|---|
| bf16 | Intel、AMD、ARM、NVIDIAが対応。指数はf32と同じ、仮数7ビット |
| fp8 | E4M3／E5M2が主流。DeepSeek-V3が671BのMoEをFP8で訓練した |
| fp4 | NVIDIA Blackwellが第5世代Tensor CoreでNVFP4／MXFP4を直接支える |
| MX | OCPの標準。32要素でE8M0スケールを共有。要素はMXFP4／6/8 |
| 見通し | 2026後半から2027にかけて、FP4の対応が広がり、MXの標準化が進む |

**情勢は「狭い浮動小数点＋ブロックスケール」に向かっている。**
f64の重い演算ではなく、bf16、fp8、fp4が実装されている。

## 5. 採否

### 5.1 採る候補

**IEEE 754の系（f16／f32／f64）を土台に置く。**
ビット表現が確定し、Rustが`f32`／`f64`を、`half`が`f16`を持つ。
模擬実行はRustの演算にそのまま載る。
相互変換ではVerylの`f32`／`f64`と正確に対応する。

**bf16とfp8（E4M3／E5M2）を機械学習向けに加える。**
情勢がこの形式に集まっている。
`half`クレートがbf16を持つ。fp8はビット幅が小さく、模擬実行で表を引く実装も現実的である。

### 5.2 保留する候補

**fp4とMXは、単一の型としては保留する。**
fp4の要素型は加えてよいが、MXはブロックとスケールの組であり、
基本型ではなく`struct`と配列で表すべきである。
言語に基本型として入れるより、ライブラリの型として定義するほうが素直である。

### 5.3 採らない候補

**positは採らない。**
Verylは持つが、情勢はpositに向かっていない。
機械学習はMX／fp8／fp4へ進んだ。
positはregimeが可変で、ハードウェアの実装が重く、IEEEと別系統である。
**IRISが持つ意義が薄い。** Verylからの変換では`Unsupported`のままにする。

### 5.4 まとめ

| 形式 | 採否 |
|---|---|
| f32／f64 | **採る（土台）** |
| f16 | 採る |
| bf16 | **採る（機械学習）** |
| fp8 E4M3／E5M2 | **採る（機械学習）** |
| fp4（要素型） | 加えてよい |
| MX（ブロック） | 基本型としては保留。`struct`と配列で表す |
| posit | 採らない |

## 6. 実装したら何が要るか

**実装は今回やらない。** 実装するなら次が要る。

| 対象 | 変更 |
|---|---|
| `spec/` | 浮動小数点の型と実数リテラルを言語仕様に足す |
| `tools/iris.ebnf` | 型と実数リテラルの規則を足す（**第3の義務が発火する**） |
| `iris-sim`の値システム | 現在ビットベクタのみ。浮動小数点の値の表現が要る |
| 模擬実行 | 各形式の演算。IEEEはRustの演算、狭い形式は変換と丸め |
| `iris2sv` | SystemVerilogの`real`か、ビット列としての出力 |
| 形式的等価性 | 浮動小数点はビット単位でしか等価性を言えない |

**値システムへの変更は、保留中の`string`と同じ根に触れる。**
`iris-sim`の値がビットベクタしか持たないことが、両方の壁である。

## 7. やらなかったこと

- **実装していない。** 型も演算も追加していない
- `spec/`と`tools/iris.ebnf`に触っていない
- 性能や面積は測っていない。情勢の事実は公表資料による
- MXのブロック形式を型としてどう定義するかは、方針の提示にとどめた
- 各形式の丸めの規則の細部は詰めていない

## 出典

情勢の事実は次から取った（2026-08-22に参照）。

- [Veryl Simulator: Performance Comparison with Verilator](https://veryl-lang.org/blog/veryl-simulator-performance/)
- [The Low-Precision Edge: FP8 and FP4 — Summer 2026 Guide](https://javaeeeee.medium.com/low-precision-ai-format-comparisonthe-low-precision-edge-fp8-and-fp4-in-pytorch-jax-tensorflow-19bfbcb13438)
- [An Empirical Study of Microscaling Formats for Low-Precision LLM Training](https://aisystemcodesign.github.io/papers/FP4.pdf)
- [An 83-Format Numeric Catalog with Bit-Exact Conformance Vectors](https://arxiv.org/pdf/2606.09686)
