//! Signal trace for waveform recording
//!
//! The recorder lives in `iris-runtime` so that a compiled simulation writes
//! the same waveform as the interpreter.

pub use iris_runtime::trace::SignalTrace;
